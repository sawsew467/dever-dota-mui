import { Container, Typography } from "@mui/material";
import React from "react";

const Home = () => {
  return (
    <Container>
      <Typography variant={"h6"}>WELCOME TO</Typography>
      <Typography variant={"h1"}>DOTA 2 MUI</Typography>
    </Container>
  );
};

export default Home;
